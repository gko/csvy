{
  "name": "csvy",
  "version": "0.3.0",
  "description": "yaml/json converter to csv",
  "main": "index.js",
  "bin": "./bin/csvy",
  "scripts": {
    "build": "./node_modules/.bin/tsc -t ES6 --module commonjs --moduleResolution node index.ts ./bin/csvy.ts --outFile ./bin/csvy",
    "test": "npm run build && ./node_modules/.bin/_mocha",
    "watch": "npm run test-watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "^3.7.0",
    "mocha": "^3.2.0",
    "typescript": "^2.1.5"
  },
  "devDependencies": {
    "@types/node": "^7.0.0"
  }
}
